type: object
properties:
  _id:
    type: string
    format: uuid
  _IdentityId:
    type: string
    format: uuid
  _appointmentIds:
    type: array
    items:
      $ref: './AppointmentId.yaml'
  _reviewIds:
    type: array
    items:
      $ref: './ReviewId.yaml'
  notAvilableDates:
    type: array
    items:
      $ref: './Unavailability.yaml'
  services:
    $ref: './Services.yaml'
  billingSettings:
    $ref: './WorkerBillingSettings.yaml'
